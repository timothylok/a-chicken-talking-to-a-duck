# Session log

## 2026-07-11
- Filled CLAUDE.md section 10 with the voice-OS architecture blueprint; added memory/session-log paths.
- Design review: added prioritized hardening checklist to CLAUDE.md (tunnel auth bypass, command-router injection, upload limits, reliability).
- Built the Vercel gateway in `gateway/`: `api/voice.ts` with timing-safe bearer auth, content-type + 4 MB size validation, Cloudflare Access service-token headers on the upstream call, response normalization, 55 s upstream timeout. Typechecked and smoke-tested (8/8 pass) against a stub ASR server.
- Gateway deployed to https://a-chicken-talking-to-a-duck.vercel.app (repo: https://github.com/timothylok/a-chicken-talking-to-a-duck, root dir `gateway/`, preset "Other").
- Built the local ASR service in `asr/`: FastAPI + faster-whisper on 127.0.0.1:9000, `POST /inference` (multipart or raw audio), `/health`, in-memory only (no disk writes, no client filenames), 16 MB / 120 s caps, single-inference lock. GPU (GTX 1650) works via nvidia-cublas-cu12/nvidia-cudnn-cu12 pip wheels; startup warmup forces cuBLAS load so CPU fallback actually triggers. Verified end-to-end with Windows TTS speech on CUDA.
- Model note: 4 GB VRAM fits `medium` (default) but not `large-v3` float16; `yue` language token requires large-v3, so non-large-v3 models auto-downgrade to `zh`. Benchmark before locking model choice.
- ASR service committed and pushed (cbebb68).
- Cloudflare Tunnel set up: cloudflared 2026.7.1 installed via winget, tunnel `voice-asr` (77c3012c-98ad-4e5d-83c0-80e3423fcc40), DNS voice.fittertrack.com → localhost:9000, config at C:\Users\timlo\.cloudflared\config.yml. Domain is fittertrack.com (blueprint's tt-tunnel.com was a placeholder). Verified end-to-end: /health and /inference through the public URL, ~1.9 s for 2 s of audio on medium/CUDA.
- Vercel env vars set (VOICE_GATEWAY_KEY, ASR_URL); local copy in gateway/.env (gitignored). Fixed gateway crash on Vercel: web-handler export needs "type":"module" in package.json (cd10845).
- FULL CHAIN VERIFIED: POST /api/voice with bearer key → Vercel → tunnel → Win11 GPU ASR → transcription, 3.6 s for 2 s audio; 401 without key.
- Cloudflare Access set up via API (Zero Trust org onboarded first): app `voice-asr` on voice.fittertrack.com, Service Auth policy, service token `voice-gateway` (1 yr). Anonymous → 401 at edge; CF_ACCESS_* creds in gateway/.env and Vercel env. Full chain retested with Access enforced: 200 in 3.9 s. Checklist item 1 closed.
- User advised to roll the Cloudflare API token pasted in chat.
- Windows services installed (checklist item ticked): `Cloudflared` (explicit --config/--logfile in binPath; the systemprofile config-discovery route crash-looped) and `VoiceASR` (NSSM). Two porting bugs fixed: (1) Store Python unusable by services (MSIX container) → installed python.org 3.12 machine-wide, rebuilt venv; (2) ctranslate2 finds cuBLAS via PATH search, not add_dll_directory → server.py now prepends nvidia bin dirs to PATH. AC sleep/hibernate disabled. Verified: both services auto-start + running, health on CUDA, full chain 200 in 4.6 s. server.py PATH fix NOT yet committed.
- Command router built (0b441db): `asr/router.py` — exact normalized-phrase allowlist (traditional + simplified + English variants), destructive commands need 確認/confirm within 60 s (取消/cancel clears). Endpoint `POST /command` on the ASR service (transcribe → route → execute → reply); gateway exposes it as `/api/voice?mode=command` (command responses pass through un-normalized). Commands: SYSTEM_STATUS (safe), TRIGGER_DEPLOY (destructive, needs DEPLOY_HOOK_URL env on the VoiceASR service). 12 router unit tests pass; full chain verified: spoken "system status" → executed → reply, through gateway + Access + tunnel. Checklist items 2 (allowlist) and 3 (confirmation) ticked.
- Next: iPhone client (iOS Shortcut), external uptime ping, add real commands (Notion, quant jobs) + DEPLOY_HOOK_URL.

## 2026-07-12
- iPhone Shortcut recipe written (iphone-shortcut.md, 7a56796) and debugged live: first failures were Text-type form fields (`{'file': 'str', 'File': 'str'}`), then a broken header after editing. Added rejection-reason logging (server) + upstream error detail passthrough (gateway) + error branch in the recipe (ed7e9cb) — failures are now spoken aloud, never silent.
- MILESTONE: system fully operational with real Cantonese. User spoke 系統狀態 into the Shortcut → SYSTEM_STATUS executed → Cantonese reply spoken back. Free-form Cantonese transcribes well (講嘢 came through correctly under zh/medium).
- Feedback-channel checklist item ticked.
- New system commands: RESTART_ASR (重啟語音系統 — replies, then exits so NSSM relaunches; can't SCM-restart itself) and richer SYSTEM_STATUS/health check (檢查系統狀態/健康檢查 — model, device, uptime in Chinese). Phrase variants added from real transcriptions (Whisper translated "health check" → 健康檢查).
- Ollama chat fallback: unmatched phrases go to local Ollama (qwen2.5:7b default, OLLAMA_URL/OLLAMA_MODEL env) and the English reply is spoken back. Kept exact-match allowlist (rejected pasted substring-match version — would let embedded 重新部署 trigger deploys), stdlib urllib (no requests dep), and moved route() into threadpool (30 s LLM call would block event loop). Fallback is reply-only — chat can never trigger commands. Live-verified: command / chat / voice-restart all work; NSSM relaunched service after voice restart.
- Chat fallback moved to pure Cantonese: qwen2.5:7b mixed English/emoji into replies ("今日 weather 好似good啦！👋🏻", "Rihanna"); model comparison found gemma3:4b produces genuine 口語 (qwen3:8b gives 書面語). Switched default to gemma3:4b via /api/chat with system prompt; strips <think> tags for thinking models; chat replies now logged (cf6fe85).
- Accuracy pass from real logs (af384bb): added observed misses 系統狀況/System Health to SYSTEM_STATUS; new LIST_COMMANDS command (有咩指令 — self-generating from COMMANDS); Whisper initial_prompt built from command vocabulary biases decoding toward exact allowlisted phrases.
- Declined pasted "Agnes AI" language-routing design: Agnes undefined (no endpoint/auth), and forced language=zh means English speech often arrives pre-translated so text-based language detection would misroute. User dropped it.
- Heartbeat monitoring: `ops/heartbeat.ps1` checks local ASR /health + Cloudflared service, pings healthchecks.io (check a221e72c…, 30-min period) or POSTs reason to /fail. Scheduled task "VoiceOS Heartbeat" (SYSTEM, every 10 min) registered and verified. Checklist uptime item ticked.
- Next: add real commands (Notion, quant jobs, DEPLOY_HOOK_URL), rate limiting, key-rotation procedure.

## 2026-07-13
- Heartbeat validated in real life: server shutdown on 2026-07-12 triggered the healthchecks.io email as designed.
- Root-caused nonsense weather reply: 今日天氣如何 matched no command → gemma3:4b chat fallback hallucinated (no weather data); today's retry hit an Ollama 30 s timeout (chat_error). Log mining note: service.log stores CJK as \uXXXX escapes — grep escaped forms, not raw characters.
- New WEATHER_TODAY command (asr/router.py): Open-Meteo API (keyless) for Auckland, New Zealand — user's location, NOT Hong Kong. Current temp + WMO code → spoken-Cantonese description + today's high/low/rain probability. Phrases include the real logged form 今日天氣如何. Route-tested locally: correct Cantonese reply with live data.
- Voice restart failed first: Whisper produced mixed-script 重啟语音系统 (traditional 啟 + simplified 语音系统), not in the exact-match allowlist → chat fallback → another Ollama timeout. Added the mixed form to RESTART_ASR phrases. Ollama timeout root cause: gemma3:4b cold-load after the shutdown exceeds the router's 30 s limit; warm it replies in ~9 s.
- Service restarted via elevated Start-Process (Restart-Service needs admin; service runs as SYSTEM — Claude's shell can trigger UAC with Start-Process -Verb RunAs). LIVE-VERIFIED: 今日天氣 → WEATHER_TODAY executed through the full chain, ~3 s.
- Public Cantonese home page at https://a-chicken-talking-to-a-duck.vercel.app/ (fb48142): `gateway/public/index.html` served statically by Vercel ("Other" preset serves public/ alongside api/ functions). Two sections: purpose (口語廣東話) + command table (primary phrase, description, alt phrases, ⚠確認 badge for destructive).
- Page auto-syncs with COMMANDS: `ops/generate_homepage.py` renders the HTML from asr/router.py (deterministic output; per-command descriptions live in the script's DESCRIPTIONS dict — add a line there for each new command). Pre-commit hook `ops/githooks/pre-commit` regenerates + stages the page whenever router.py is committed. Hook enabled via `git config core.hooksPath ops/githooks` (machine-local — rerun on a fresh clone). Hook fired live during the commit; page verified deployed; /api/voice still 401s without key.
- Installed 102 TheColab NZ data skills (github.com/thecolab-ai/.skills) after vetting: scanned all network destinations (NZ govt/public only), env reads (service API keys only), SKILL.md injection patterns (clean), subprocess use (pptx/LibreOffice only). Clone at D:\ai\thecolab-skills, junctioned into .claude/skills (junctions preserve the parents[3] lib/nzfetch.py resolution); dir gitignored. Live-verified geonet-nz + nz-news + junction lib resolution. Declined the pasted "install with no review, keep alive for 30 min" loop framing: skills are on-demand CLIs, not daemons — nothing to babysit, and unreviewed third-party skill installs are a supply-chain risk.
- FUEL_PRICES voice command (asr/router.py): subprocess to the vetted petrolmate-nz-au skill CLI (keyless Gaspy data, ~0.6 s), cheapest three 91-octane stations spoken in Cantonese (油價/入油/fuel price...). First COMMANDS entry backed by a TheColab skill. Homepage DESCRIPTIONS updated; route-tested locally; service restarted (elevated) and live (6f6da96, homepage deployed).
- Fuel reply v2 after live feedback ("location not clear"): station street addresses in the reply; search centered on FUEL_LOCATION service env var ("glenfield auckland" — MUST disambiguate, bare "glenfield" resolves to Glenfield NSW in Sydney); executed command replies now logged to service.log (server.py only logged transcription+command ID). Ops gotcha: `nssm set` through Start-Process quoting mangles quoted values — write the AppEnvironmentExtra REG_MULTI_SZ directly via Set-ItemProperty with -EncodedCommand instead; elevated script logs OK/FAILED to a result file. Verified: registry has HF_HOME + FUEL_LOCATION, service healthy.
- Fuel reply v3 (2819387): 95-octane (PULP95), station+price only, cheapest first. Log readability fixed: NSSM stderr is cp1252 so CJK logged as \uXXXX escapes — sys.stderr.reconfigure(encoding="utf-8") in server.py; verified end-to-end by POSTing a Windows-TTS "system status" wav to /command and reading the readable log line.
- BUS_TIMES voice command (巴士...): at-transport skill (keyless for stops/departures), queries BUS_STOPS env (default 3881,4010 = Glenfield Mall both directions), next 3 buses by minutes-away from GTFS-RT expected_time (scheduled_time fallback via Pacific/Auckland). Caveat: AT feed is realtime-only — between services the reply says no bus is being tracked. Needed tzdata pip package (Windows Python has no tz database) in BOTH asr/.venv (what VoiceASR actually runs — nssm Application is .venv\Scripts\python.exe) and system Python312. Positive path live-verified ("917號即刻到").
- All command replies pass _pause_english(): Chinese comma between adjacent English words so iOS TTS pauses (user request). Log-viewing note: service.log is UTF-8; PowerShell 5.1 needs `Get-Content -Encoding UTF8`.
- TIDE_TIMES voice command (潮汐...): nz-tides-surf skill, LINZ predictions for TIDE_PORT env (default auckland; surf aliases like piha work). Next high+low with _speak_time() Cantonese phrasing (18:15 → 夜晚6點15分 — raw HH:MM reads badly in TTS). No data gaps (predictions, not realtime). Live-verified.
- BIN_DAY voice command (垃圾/幾時收垃圾...): auckland-bin-schedule skill. BIN_ADDRESS service env holds the numeric council property ID (12340757627 = user's home) — privacy: no address in git, registry, or spoken reply. Per-property gotcha: bare "glenfield auckland" fuzzy-matches Glenfield Road in PAPAKURA — always resolve to a property ID with `--list` first. Dates spoken via _speak_date ("Thursday, 16 July" → 禮拜四7月16號), streams sharing a date merge (垃圾同廚餘同回收...). Live-verified.
- CLAUDE.md synced to as-built (9 commands, service env vars, skills layer, home page) and its "Current commands" line is now a marker block (<!-- COMMANDS:BEGIN/END -->) auto-rewritten by ops/generate_homepage.py; pre-commit hook stages CLAUDE.md alongside the home page on router.py commits.
- NEWS_HEADLINES voice command (新聞...): nz-news skill, top 3 NZ headlines. First run under the service failed ("Expecting value: line 1 column 1") — fourth cp1252 incident: skill child's stdout defaulted to cp1252 and macrons (rūnanga) killed it with UnicodeEncodeError before printing; local tests masked it because PYTHONIOENCODING was exported in the shell. Fix: shared _run_skill() helper forces PYTHONIOENCODING=utf-8 on ALL skill subprocesses; all five skill commands refactored onto it. Per-command "pause_english": False opt-out (word-pause filter would mangle full English sentences).
- Headlines translated to spoken Cantonese via local Ollama (user: English TTS on iPhone sounds bad); person/place/org names stay English. Batch translation was unparseable (model returned 4 lines for 3 headlines) → one call per headline, first-line-only parse, per-headline English fallback on error, ~8 s warm total. Known limits: gemma3:4b sometimes translates a place name anyway (Marlborough→馬爾堡) and drifts on complex headlines; cold Ollama can time out (30 s/headline) → English that one time. Live-verified.
- MORNING_BRIEFING voice command (早晨/早晨簡報/good morning...): composes existing sections — weather, next buses, bin reminder, Cantonese headlines — each in its own try/except so a failed source drops out instead of killing the briefing. Bin reminder only speaks when collection is today (今日) or tomorrow (聽日): _bin_next_dates() split out of _bin_day(), date matched against the skill's "Thursday, 16 July" format built with d.strftime (no %-d/%#d — portable f-string with d.day). ~19 s locally (news translation dominates); gateway maxDuration is 60 s so fine. pause_english False (news section). Live-verified.
- Next: consider warming Ollama at boot (or longer first-call timeout) so post-reboot chat doesn't time out; add real commands (Notion, quant jobs, DEPLOY_HOOK_URL).

## 2026-07-14
- Gateway hardening completed the "upload limits" checklist item: added best-effort per-instance rate limiting (10 req/min, 429 after) to gateway/api/voice.ts, applied after auth so unauthenticated noise can't lock out the real user. Size cap + content-type validation were already in place; duration cap (120 s) lives in asr/server.py. Checklist item ticked. Typecheck passes; window logic unit-tested in Node (allow 10, block 11th, recover after 60 s).
- Ollama warm-up at startup: router.warm_ollama() preloads OLLAMA_MODEL via /api/chat with empty messages; server.py fires it in a daemon thread after Whisper loads so a slow/absent Ollama never delays ASR availability. Live-tested: gemma3:4b warmed in 7.5 s.
- Voice test: first restart attempts failed — user said 重啟語音系統+確認 in one utterance, which the exact-match router sent to chat (RESTART_ASR isn't destructive, so no 確認 needed; declined auto-stripping trailing confirms for destructive commands — would collapse the two-step guard). Command alone worked: service restarted, warm-up confirmed in log, BIN_DAY fine after restart. Committed.
- README.md written (hackathon-style sections) on request.
- Gateway rate limiting takes effect on the next push/deploy. (Pushed; live.)
- EARTHQUAKES command (地震) added via vetted geonet-nz skill (quakes --mmi 3, felt quakes only): Cantonese locality conversion ("5 km south-east of X" → "X東南面5公里"), relative time (分鐘前/個鐘前/日前), pause_english off for English place names. Voice-tested and committed.
- Notion-as-brain history designed and built: /command interactions append to asr/logs/history.jsonl (local source of truth; chat included); runners can return (reply, data) for structured comparisons (weather + news wired up — news keeps original English headlines); ops/notion_sync.py + "VoiceOS Notion Sync" scheduled task (5 min, runs as user for credential isolation) mirrors command entries only to Notion. Rejected direct-from-router Notion writes (latency + would put the key in the ASR service env). All logic tested incl. cursor idempotence; task registered and verified as unconfigured no-op. Config moved under project on request: ops/notion.json (gitignored). User created the integration + "Voice OS" page; --setup created database 語音歷史 (39da969b-9075-8141-bcee-c07f89dfde70). End-to-end verified with real voice test: BIN_DAY → history.jsonl → Notion row; chat entries correctly stayed local (2 Notion rows, 3 chats withheld).
- Log mining: 油價 misheard as 有加 (near-homophone, 1.6 s clip) and "gas price" as CASAPRICE — both added as FUEL_PRICES phrase variants, which also biases the Whisper prompt toward 油價.
- WEATHER_COMPARE (同琴日比/同尋日比) added: reads yesterday's last WEATHER_TODAY data from local history.jsonl (never Notion mid-request), fetches today live, speaks high/low deltas; graceful 冇琴日紀錄 reply when no yesterday data. Voice-tested (no-data path — first real comparison possible 2026-07-15). First feature powered by the history brain.
- CREATE_REMINDER agent built and voice-tested end-to-end: 提我/提醒我/remind-me prefix match (the one deliberate exception to exact matching — fixed-type non-destructive action, LLM output is data-only), gemma3:4b extracts {title, due} with code validation (schema, date parse, future-bounded ≤1 yr), router returns a `reminder` payload, the iPhone Shortcut creates the actual iOS Reminder (no server-side Reminders API exists). Found + fixed: gemma computes relative dates wrongly (聽日 → +2 days); pre-computed 今日/聽日/後日/禮拜X calendar in the prompt makes it copy instead of compute. A time is now required (no-time → retry reply) so the Shortcut needs no nested Ifs.
- iOS Shortcuts alert saga: "Add New Reminder" rejects every dynamic alert value on current iOS — date variable, formatted text, time-only pill, Adjust-Date-from-minutes — all "alert time provided was invalid"; only static picker values work and no date-capable alert mode exists (At Time/arrive/leave/messaging only). Final design: no alert, absolute due time in the reminder title 「買牛奶（7月15號朝早9點）」; data.due keeps the real timestamp for a future re-upgrade. Also caught: an invisible trailing space in a Shortcuts dictionary key sent every response to the error branch — documented in iphone-shortcut.md troubleshooting, which now opens with a full both-flows action list.
- FUEL_PRICES and BIN_DAY now return (reply, data) — stations+prices / per-stream collection dates — so their Notion Data column populates for future comparisons.
- Log mining round 2: 巴士 misheard as 巴西; restart said naturally as 重置語音系統/系統重置/重启系統 (incl. mixed-script form) — all added as phrase variants.
- Next: key hygiene (rotation, replay bounding), idempotency keys, watch translation name-hallucination (Middlemore→沙田).

## 2026-07-15
- Verified WEATHER_COMPARE's first real comparison: yesterday's WEATHER_TODAY data (high 17/low 9) is in history.jsonl; runner invoked directly returned real deltas — 今日最高15度，比琴日低2度，最低8度，比琴日低1度. Ready for the first real voice use.
- Verified Notion Data column: WEATHER_TODAY rows populate (today's + yesterday's confirmed via API query). FUEL_PRICES/BIN_DAY Notion rows are empty only because the logged entries predate f23f28e; both runners invoked directly now return structured data ({fuel, stations[]} / per-stream dates), so the next voice use will populate.
- Key hygiene checklist item done: timing-safe compare was already in gateway/api/voice.ts; added required X-Timestamp header (ISO 8601, ±5 min skew) to bound replay, and documented the VOICE_GATEWAY_KEY rotation procedure in CLAUDE.md (Security model). iphone-shortcut.md updated (header setup + troubleshooting rows).
- Idempotency checklist item done: SHA-256 body dedupe at the gateway — identical bytes within 60 s → 409 duplicate. Catches network-level retries/double-sends; separate recordings differ by design. Per-instance best-effort like the rate limit.
- Both changes typechecked and exercised in Node (bad key 401, missing/stale/garbage timestamp 401, fresh passes, identical resend 409, new body passes).
- NOT committed/pushed. ⚠️ Deploying is a breaking change for the current Shortcut: add the X-Timestamp header (iphone-shortcut.md step 2) at the same time as pushing, or every run 401s.
- Log-mining check: last night's 18:xx misses (系统重置, 巴西 → chat) all predate f23f28e, whose variants are live (service restarted ~21:30). Nothing new to add. No NEWS_HEADLINES runs since the 沙田醫院 hallucination — still watching.
- User reported mid-session: BUS_TIMES had no Data on Notion — its runner never returned structured data (only weather/fuel/bin did). _bus_times now returns (reply, data) with stop + top-3 {route, minutes}; verified directly (Glenfield Mall, 906 即刻到). User restarted the service by voice, so it's live. TIDE_TIMES got the same treatment on request right after ({port, events[{type, time, height_m}]}, verified directly) — every skill command now returns structured data.
- Deployed: user approved commit+push; gateway hardening + bus data live, X-Timestamp check verified in production (curl probe got the new 401), user updated the Shortcut. First real WEATHER_COMPARE spoken by voice (天氣比較 09:59). TIDE_TIMES data live after voice restart (潮水 10:40 with events).
- Homepage: added "Translate to English" Google Translate link via ops/generate_homepage.py (survives hook regeneration; verified 302 to the translate.goog proxy). Google's embeddable widget is discontinued — the link was the right option.
- Log mining round 3 (user request after 6 failed tide attempts 10:38–10:40): added TIDE_TIMES variants — 潮漲潮退/何時潮漲/何時潮退 (natural forms), 超值/超值時間/何時朝退/幾時朝章/撤退 (observed mishearings; 撤退 is a real word but the command wins, 巴西 precedent), simplified 潮涨/潮汐时间/潮涨潮退. All verified to route.
- ASR benchmark built (user asked about Whisper fine-tuning; agreed benchmark-first): server.py gets opt-in ASR_CAPTURE_DIR (saves request audio + transcript locally, warning logged when on); ops/asr_bench.py transcribes captured clips with each model (same prompt/VAD as service, CPU by default to spare the GPU) and reports command-routing accuracy, exact match, CER. Candidate JackyHoCL/whisper-small-cantonese-yue-english-ct2 (Cantonese+English code-switching fine-tune, ready CT2 — no torch needed) downloaded and smoke-tested alongside medium. Real run needs the user to capture ~20 spoken phrases first. Fine-tuning on own voice deferred: 4 GB GPU can't train; only if the community model disappoints.
- Benchmark ran on 12 real phone clips (capture setup hit two snags: user's first registry paste ran lines in reverse order — fixed with a one-liner; and the first phrase pass was all 13 phrases in one recording — needs one per Shortcut run). Two ambiguous labels (發洩, C.O.T.O.) resolved by second-opinion transcription with the candidate (巴士, 潮退) + phrase-sequence position; user should flag if wrong.
- RESULT: JackyHoCL/whisper-small-cantonese-yue-english-ct2 beats medium decisively on command routing 83% vs 58%, exact 75% vs 50%, CER 0.17 vs 0.54, ~3x faster (2.6 vs 7.4 s/clip CPU). Its only misses were one-char near-misses 油家/潮推 — added as variants (+朝夕 observed 10:58), after which it routes 12/12. medium's misses were unusable (發洩, 確認(!), 散漫, 巴適, C.O.T.I).
- Caveats: 12 clips, one speaker, commands only — code-switching and long chat utterances untested (though the fine-tune trained on mixed Canto-English speech). Recommended switching ASR_MODEL to the fine-tune; keep capture on until confirmed in daily use, then remove ASR_CAPTURE_DIR + delete asr/logs/capture (log-hygiene item).
- User switched production: ASR_MODEL=JackyHoCL/whisper-small-cantonese-yue-english-ct2, service healthy on CUDA. README gained the mishearing-challenge/benchmark-strategy story; lesson saved to memory (asr-mishearing-lessons).
- Post-switch verification by replaying the bench clips through the live service found a subtle trap: GPU int8_float16 transcribes marginal clips differently from the CPU int8 bench — 潮汐 clip came out 潮漬 live (超值 in the bench), 油價 clip 油㗎 (油家 in bench). Both added as variants; all replayed clips now route. Lesson: after a model/precision change, replay the test clips through the LIVE service, not just the bench.
- MILK_PRICES built (planned in plan mode from a user-pasted over-engineered "price intelligence" spec, scoped to: cheapest standard 3L per store + briefing-only drop alert): grocer-nz skill via new SQL `query` subcommand, stores 45/246/315/365 (Woolworths Glenfield, PAK'nSAVE Albany, New World Albany+Birkenhead), product whitelist 5449/4068/5506/5456 (Pams/WW/Simply standard + Anchor Blue Top fallback), env-overridable MILK_STORES/MILK_PRODUCTS. Reply dedupes chains (cheapest branch spoken, all in data). _milk_drop_line() in MORNING_BRIEFING speaks only when cheapest < last pre-today grocer history row — stateless, no local storage. duckdb+pytz installed into asr/.venv (grocer CLI's uv auto-bootstrap crashes 0xC0000005 under system Python). Gotchas: CLI caches parquet forever → must pass --refresh, and it's a main-parser flag (before the subcommand). Verified: real prices (PnS $7.09 cheapest), drop branch via monkeypatched history, all phrases route, briefing 21 s with milk line correctly silent. grocer.nz has no Costco; "Countdown" is Woolworths now. Deferred by user: Δ-vs-yesterday in reply, 30-day trend command, always-on briefing line.
- Next: user restarts service (voice works) to load MILK_PRICES + the 潮漬/油㗎 variants, voice-test 牛奶價錢 and mine its transcriptions; daily-drive the new model with capture on; if chat quality holds for a day or two, remove ASR_CAPTURE_DIR + delete asr/logs/capture. Then credential isolation, file handling safety, secrets/audio out of logs; DEPLOY_HOOK_URL still unconfigured.
