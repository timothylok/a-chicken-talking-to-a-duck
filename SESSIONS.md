# Session log

## 2026-07-11
- Filled CLAUDE.md section 10 with the voice-OS architecture blueprint; added memory/session-log paths.
- Design review: added prioritized hardening checklist to CLAUDE.md (tunnel auth bypass, command-router injection, upload limits, reliability).
- Built the Vercel gateway in `gateway/`: `api/voice.ts` with timing-safe bearer auth, content-type + 4 MB size validation, Cloudflare Access service-token headers on the upstream call, response normalization, 55 s upstream timeout. Typechecked and smoke-tested (8/8 pass) against a stub ASR server.
- Gateway deployed to https://a-chicken-talking-to-a-duck.vercel.app (repo: https://github.com/timothylok/a-chicken-talking-to-a-duck, root dir `gateway/`, preset "Other").
- Built the local ASR service in `asr/`: FastAPI + faster-whisper on 127.0.0.1:9000, `POST /inference` (multipart or raw audio), `/health`, in-memory only (no disk writes, no client filenames), 16 MB / 120 s caps, single-inference lock. GPU (GTX 1650) works via nvidia-cublas-cu12/nvidia-cudnn-cu12 pip wheels; startup warmup forces cuBLAS load so CPU fallback actually triggers. Verified end-to-end with Windows TTS speech on CUDA.
- Model note: 4 GB VRAM fits `medium` (default) but not `large-v3` float16; `yue` language token requires large-v3, so non-large-v3 models auto-downgrade to `zh`. Benchmark before locking model choice.
- ASR service committed and pushed (cbebb68).
- Cloudflare Tunnel set up: cloudflared 2026.7.1 installed via winget, tunnel `voice-asr` (77c3012c-98ad-4e5d-83c0-80e3423fcc40), DNS voice.fittertrack.com → localhost:9000, config at C:\Users\timlo\.cloudflared\config.yml. Domain is fittertrack.com (blueprint's tt-tunnel.com was a placeholder). Verified end-to-end: /health and /inference through the public URL, ~1.9 s for 2 s of audio on medium/CUDA.
- Vercel env vars set (VOICE_GATEWAY_KEY, ASR_URL); local copy in gateway/.env (gitignored). Fixed gateway crash on Vercel: web-handler export needs "type":"module" in package.json (cd10845).
- FULL CHAIN VERIFIED: POST /api/voice with bearer key → Vercel → tunnel → Win11 GPU ASR → transcription, 3.6 s for 2 s audio; 401 without key.
- Cloudflare Access set up via API (Zero Trust org onboarded first): app `voice-asr` on voice.fittertrack.com, Service Auth policy, service token `voice-gateway` (1 yr). Anonymous → 401 at edge; CF_ACCESS_* creds in gateway/.env and Vercel env. Full chain retested with Access enforced: 200 in 3.9 s. Checklist item 1 closed.
- User advised to roll the Cloudflare API token pasted in chat.
- Windows services installed (checklist item ticked): `Cloudflared` (explicit --config/--logfile in binPath; the systemprofile config-discovery route crash-looped) and `VoiceASR` (NSSM). Two porting bugs fixed: (1) Store Python unusable by services (MSIX container) → installed python.org 3.12 machine-wide, rebuilt venv; (2) ctranslate2 finds cuBLAS via PATH search, not add_dll_directory → server.py now prepends nvidia bin dirs to PATH. AC sleep/hibernate disabled. Verified: both services auto-start + running, health on CUDA, full chain 200 in 4.6 s. server.py PATH fix NOT yet committed.
- Command router built (0b441db): `asr/router.py` — exact normalized-phrase allowlist (traditional + simplified + English variants), destructive commands need 確認/confirm within 60 s (取消/cancel clears). Endpoint `POST /command` on the ASR service (transcribe → route → execute → reply); gateway exposes it as `/api/voice?mode=command` (command responses pass through un-normalized). Commands: SYSTEM_STATUS (safe), TRIGGER_DEPLOY (destructive, needs DEPLOY_HOOK_URL env on the VoiceASR service). 12 router unit tests pass; full chain verified: spoken "system status" → executed → reply, through gateway + Access + tunnel. Checklist items 2 (allowlist) and 3 (confirmation) ticked.
- Next: iPhone client (iOS Shortcut), external uptime ping, add real commands (Notion, quant jobs) + DEPLOY_HOOK_URL.

## 2026-07-12
- iPhone Shortcut recipe written (iphone-shortcut.md, 7a56796) and debugged live: first failures were Text-type form fields (`{'file': 'str', 'File': 'str'}`), then a broken header after editing. Added rejection-reason logging (server) + upstream error detail passthrough (gateway) + error branch in the recipe (ed7e9cb) — failures are now spoken aloud, never silent.
- MILESTONE: system fully operational with real Cantonese. User spoke 系統狀態 into the Shortcut → SYSTEM_STATUS executed → Cantonese reply spoken back. Free-form Cantonese transcribes well (講嘢 came through correctly under zh/medium).
- Feedback-channel checklist item ticked.
- New system commands: RESTART_ASR (重啟語音系統 — replies, then exits so NSSM relaunches; can't SCM-restart itself) and richer SYSTEM_STATUS/health check (檢查系統狀態/健康檢查 — model, device, uptime in Chinese). Phrase variants added from real transcriptions (Whisper translated "health check" → 健康檢查).
- Ollama chat fallback: unmatched phrases go to local Ollama (qwen2.5:7b default, OLLAMA_URL/OLLAMA_MODEL env) and the English reply is spoken back. Kept exact-match allowlist (rejected pasted substring-match version — would let embedded 重新部署 trigger deploys), stdlib urllib (no requests dep), and moved route() into threadpool (30 s LLM call would block event loop). Fallback is reply-only — chat can never trigger commands. Live-verified: command / chat / voice-restart all work; NSSM relaunched service after voice restart.
- Chat fallback moved to pure Cantonese: qwen2.5:7b mixed English/emoji into replies ("今日 weather 好似good啦！👋🏻", "Rihanna"); model comparison found gemma3:4b produces genuine 口語 (qwen3:8b gives 書面語). Switched default to gemma3:4b via /api/chat with system prompt; strips <think> tags for thinking models; chat replies now logged (cf6fe85).
- Accuracy pass from real logs (af384bb): added observed misses 系統狀況/System Health to SYSTEM_STATUS; new LIST_COMMANDS command (有咩指令 — self-generating from COMMANDS); Whisper initial_prompt built from command vocabulary biases decoding toward exact allowlisted phrases.
- Declined pasted "Agnes AI" language-routing design: Agnes undefined (no endpoint/auth), and forced language=zh means English speech often arrives pre-translated so text-based language detection would misroute. User dropped it.
- Heartbeat monitoring: `ops/heartbeat.ps1` checks local ASR /health + Cloudflared service, pings healthchecks.io (check a221e72c…, 30-min period) or POSTs reason to /fail. Scheduled task "VoiceOS Heartbeat" (SYSTEM, every 10 min) registered and verified. Checklist uptime item ticked.
- Next: add real commands (Notion, quant jobs, DEPLOY_HOOK_URL), rate limiting, key-rotation procedure.

## 2026-07-13
- Heartbeat validated in real life: server shutdown on 2026-07-12 triggered the healthchecks.io email as designed.
- Root-caused nonsense weather reply: 今日天氣如何 matched no command → gemma3:4b chat fallback hallucinated (no weather data); today's retry hit an Ollama 30 s timeout (chat_error). Log mining note: service.log stores CJK as \uXXXX escapes — grep escaped forms, not raw characters.
- New WEATHER_TODAY command (asr/router.py): Open-Meteo API (keyless) for Auckland, New Zealand — user's location, NOT Hong Kong. Current temp + WMO code → spoken-Cantonese description + today's high/low/rain probability. Phrases include the real logged form 今日天氣如何. Route-tested locally: correct Cantonese reply with live data.
- Voice restart failed first: Whisper produced mixed-script 重啟语音系统 (traditional 啟 + simplified 语音系统), not in the exact-match allowlist → chat fallback → another Ollama timeout. Added the mixed form to RESTART_ASR phrases. Ollama timeout root cause: gemma3:4b cold-load after the shutdown exceeds the router's 30 s limit; warm it replies in ~9 s.
- Service restarted via elevated Start-Process (Restart-Service needs admin; service runs as SYSTEM — Claude's shell can trigger UAC with Start-Process -Verb RunAs). LIVE-VERIFIED: 今日天氣 → WEATHER_TODAY executed through the full chain, ~3 s.
- Public Cantonese home page at https://a-chicken-talking-to-a-duck.vercel.app/ (fb48142): `gateway/public/index.html` served statically by Vercel ("Other" preset serves public/ alongside api/ functions). Two sections: purpose (口語廣東話) + command table (primary phrase, description, alt phrases, ⚠確認 badge for destructive).
- Page auto-syncs with COMMANDS: `ops/generate_homepage.py` renders the HTML from asr/router.py (deterministic output; per-command descriptions live in the script's DESCRIPTIONS dict — add a line there for each new command). Pre-commit hook `ops/githooks/pre-commit` regenerates + stages the page whenever router.py is committed. Hook enabled via `git config core.hooksPath ops/githooks` (machine-local — rerun on a fresh clone). Hook fired live during the commit; page verified deployed; /api/voice still 401s without key.
- Installed 102 TheColab NZ data skills (github.com/thecolab-ai/.skills) after vetting: scanned all network destinations (NZ govt/public only), env reads (service API keys only), SKILL.md injection patterns (clean), subprocess use (pptx/LibreOffice only). Clone at D:\ai\thecolab-skills, junctioned into .claude/skills (junctions preserve the parents[3] lib/nzfetch.py resolution); dir gitignored. Live-verified geonet-nz + nz-news + junction lib resolution. Declined the pasted "install with no review, keep alive for 30 min" loop framing: skills are on-demand CLIs, not daemons — nothing to babysit, and unreviewed third-party skill installs are a supply-chain risk.
- FUEL_PRICES voice command (asr/router.py): subprocess to the vetted petrolmate-nz-au skill CLI (keyless Gaspy data, ~0.6 s), cheapest three 91-octane stations spoken in Cantonese (油價/入油/fuel price...). First COMMANDS entry backed by a TheColab skill. Homepage DESCRIPTIONS updated; route-tested locally; service restarted (elevated) and live (6f6da96, homepage deployed).
- Fuel reply v2 after live feedback ("location not clear"): station street addresses in the reply; search centered on FUEL_LOCATION service env var ("glenfield auckland" — MUST disambiguate, bare "glenfield" resolves to Glenfield NSW in Sydney); executed command replies now logged to service.log (server.py only logged transcription+command ID). Ops gotcha: `nssm set` through Start-Process quoting mangles quoted values — write the AppEnvironmentExtra REG_MULTI_SZ directly via Set-ItemProperty with -EncodedCommand instead; elevated script logs OK/FAILED to a result file. Verified: registry has HF_HOME + FUEL_LOCATION, service healthy.
- Fuel reply v3 (2819387): 95-octane (PULP95), station+price only, cheapest first. Log readability fixed: NSSM stderr is cp1252 so CJK logged as \uXXXX escapes — sys.stderr.reconfigure(encoding="utf-8") in server.py; verified end-to-end by POSTing a Windows-TTS "system status" wav to /command and reading the readable log line.
- BUS_TIMES voice command (巴士...): at-transport skill (keyless for stops/departures), queries BUS_STOPS env (default 3881,4010 = Glenfield Mall both directions), next 3 buses by minutes-away from GTFS-RT expected_time (scheduled_time fallback via Pacific/Auckland). Caveat: AT feed is realtime-only — between services the reply says no bus is being tracked. Needed tzdata pip package (Windows Python has no tz database) in BOTH asr/.venv (what VoiceASR actually runs — nssm Application is .venv\Scripts\python.exe) and system Python312. Positive path live-verified ("917號即刻到").
- All command replies pass _pause_english(): Chinese comma between adjacent English words so iOS TTS pauses (user request). Log-viewing note: service.log is UTF-8; PowerShell 5.1 needs `Get-Content -Encoding UTF8`.
- TIDE_TIMES voice command (潮汐...): nz-tides-surf skill, LINZ predictions for TIDE_PORT env (default auckland; surf aliases like piha work). Next high+low with _speak_time() Cantonese phrasing (18:15 → 夜晚6點15分 — raw HH:MM reads badly in TTS). No data gaps (predictions, not realtime). Live-verified.
- BIN_DAY voice command (垃圾/幾時收垃圾...): auckland-bin-schedule skill. BIN_ADDRESS service env holds the numeric council property ID (12340757627 = user's home) — privacy: no address in git, registry, or spoken reply. Per-property gotcha: bare "glenfield auckland" fuzzy-matches Glenfield Road in PAPAKURA — always resolve to a property ID with `--list` first. Dates spoken via _speak_date ("Thursday, 16 July" → 禮拜四7月16號), streams sharing a date merge (垃圾同廚餘同回收...). Live-verified.
- CLAUDE.md synced to as-built (9 commands, service env vars, skills layer, home page) and its "Current commands" line is now a marker block (<!-- COMMANDS:BEGIN/END -->) auto-rewritten by ops/generate_homepage.py; pre-commit hook stages CLAUDE.md alongside the home page on router.py commits.
- NEWS_HEADLINES voice command (新聞...): nz-news skill, top 3 NZ headlines. First run under the service failed ("Expecting value: line 1 column 1") — fourth cp1252 incident: skill child's stdout defaulted to cp1252 and macrons (rūnanga) killed it with UnicodeEncodeError before printing; local tests masked it because PYTHONIOENCODING was exported in the shell. Fix: shared _run_skill() helper forces PYTHONIOENCODING=utf-8 on ALL skill subprocesses; all five skill commands refactored onto it. Per-command "pause_english": False opt-out (word-pause filter would mangle full English sentences).
- Headlines translated to spoken Cantonese via local Ollama (user: English TTS on iPhone sounds bad); person/place/org names stay English. Batch translation was unparseable (model returned 4 lines for 3 headlines) → one call per headline, first-line-only parse, per-headline English fallback on error, ~8 s warm total. Known limits: gemma3:4b sometimes translates a place name anyway (Marlborough→馬爾堡) and drifts on complex headlines; cold Ollama can time out (30 s/headline) → English that one time. Live-verified.
- MORNING_BRIEFING voice command (早晨/早晨簡報/good morning...): composes existing sections — weather, next buses, bin reminder, Cantonese headlines — each in its own try/except so a failed source drops out instead of killing the briefing. Bin reminder only speaks when collection is today (今日) or tomorrow (聽日): _bin_next_dates() split out of _bin_day(), date matched against the skill's "Thursday, 16 July" format built with d.strftime (no %-d/%#d — portable f-string with d.day). ~19 s locally (news translation dominates); gateway maxDuration is 60 s so fine. pause_english False (news section). Live-verified.
- Next: consider warming Ollama at boot (or longer first-call timeout) so post-reboot chat doesn't time out; add real commands (Notion, quant jobs, DEPLOY_HOOK_URL).

## 2026-07-14
- Gateway hardening completed the "upload limits" checklist item: added best-effort per-instance rate limiting (10 req/min, 429 after) to gateway/api/voice.ts, applied after auth so unauthenticated noise can't lock out the real user. Size cap + content-type validation were already in place; duration cap (120 s) lives in asr/server.py. Checklist item ticked. Typecheck passes; window logic unit-tested in Node (allow 10, block 11th, recover after 60 s).
- Ollama warm-up at startup: router.warm_ollama() preloads OLLAMA_MODEL via /api/chat with empty messages; server.py fires it in a daemon thread after Whisper loads so a slow/absent Ollama never delays ASR availability. Live-tested: gemma3:4b warmed in 7.5 s.
- Voice test: first restart attempts failed — user said 重啟語音系統+確認 in one utterance, which the exact-match router sent to chat (RESTART_ASR isn't destructive, so no 確認 needed; declined auto-stripping trailing confirms for destructive commands — would collapse the two-step guard). Command alone worked: service restarted, warm-up confirmed in log, BIN_DAY fine after restart. Committed.
- README.md written (hackathon-style sections) on request.
- Gateway rate limiting takes effect on the next push/deploy. (Pushed; live.)
- EARTHQUAKES command (地震) added via vetted geonet-nz skill (quakes --mmi 3, felt quakes only): Cantonese locality conversion ("5 km south-east of X" → "X東南面5公里"), relative time (分鐘前/個鐘前/日前), pause_english off for English place names. Voice-tested and committed.
- Notion-as-brain history designed and built: /command interactions append to asr/logs/history.jsonl (local source of truth; chat included); runners can return (reply, data) for structured comparisons (weather + news wired up — news keeps original English headlines); ops/notion_sync.py + "VoiceOS Notion Sync" scheduled task (5 min, runs as user for credential isolation) mirrors command entries only to Notion. Rejected direct-from-router Notion writes (latency + would put the key in the ASR service env). All logic tested incl. cursor idempotence; task registered and verified as unconfigured no-op. Config moved under project on request: ops/notion.json (gitignored). User created the integration + "Voice OS" page; --setup created database 語音歷史 (39da969b-9075-8141-bcee-c07f89dfde70). End-to-end verified with real voice test: BIN_DAY → history.jsonl → Notion row; chat entries correctly stayed local (2 Notion rows, 3 chats withheld).
- Log mining: 油價 misheard as 有加 (near-homophone, 1.6 s clip) and "gas price" as CASAPRICE — both added as FUEL_PRICES phrase variants, which also biases the Whisper prompt toward 油價.
- WEATHER_COMPARE (同琴日比/同尋日比) added: reads yesterday's last WEATHER_TODAY data from local history.jsonl (never Notion mid-request), fetches today live, speaks high/low deltas; graceful 冇琴日紀錄 reply when no yesterday data. Voice-tested (no-data path — first real comparison possible 2026-07-15). First feature powered by the history brain.
- CREATE_REMINDER agent built and voice-tested end-to-end: 提我/提醒我/remind-me prefix match (the one deliberate exception to exact matching — fixed-type non-destructive action, LLM output is data-only), gemma3:4b extracts {title, due} with code validation (schema, date parse, future-bounded ≤1 yr), router returns a `reminder` payload, the iPhone Shortcut creates the actual iOS Reminder (no server-side Reminders API exists). Found + fixed: gemma computes relative dates wrongly (聽日 → +2 days); pre-computed 今日/聽日/後日/禮拜X calendar in the prompt makes it copy instead of compute. A time is now required (no-time → retry reply) so the Shortcut needs no nested Ifs.
- iOS Shortcuts alert saga: "Add New Reminder" rejects every dynamic alert value on current iOS — date variable, formatted text, time-only pill, Adjust-Date-from-minutes — all "alert time provided was invalid"; only static picker values work and no date-capable alert mode exists (At Time/arrive/leave/messaging only). Final design: no alert, absolute due time in the reminder title 「買牛奶（7月15號朝早9點）」; data.due keeps the real timestamp for a future re-upgrade. Also caught: an invisible trailing space in a Shortcuts dictionary key sent every response to the error branch — documented in iphone-shortcut.md troubleshooting, which now opens with a full both-flows action list.
- FUEL_PRICES and BIN_DAY now return (reply, data) — stations+prices / per-stream collection dates — so their Notion Data column populates for future comparisons.
- Log mining round 2: 巴士 misheard as 巴西; restart said naturally as 重置語音系統/系統重置/重启系統 (incl. mixed-script form) — all added as phrase variants.
- Next: key hygiene (rotation, replay bounding), idempotency keys, watch translation name-hallucination (Middlemore→沙田).

## 2026-07-15
- Verified WEATHER_COMPARE's first real comparison: yesterday's WEATHER_TODAY data (high 17/low 9) is in history.jsonl; runner invoked directly returned real deltas — 今日最高15度，比琴日低2度，最低8度，比琴日低1度. Ready for the first real voice use.
- Verified Notion Data column: WEATHER_TODAY rows populate (today's + yesterday's confirmed via API query). FUEL_PRICES/BIN_DAY Notion rows are empty only because the logged entries predate f23f28e; both runners invoked directly now return structured data ({fuel, stations[]} / per-stream dates), so the next voice use will populate.
- Key hygiene checklist item done: timing-safe compare was already in gateway/api/voice.ts; added required X-Timestamp header (ISO 8601, ±5 min skew) to bound replay, and documented the VOICE_GATEWAY_KEY rotation procedure in CLAUDE.md (Security model). iphone-shortcut.md updated (header setup + troubleshooting rows).
- Idempotency checklist item done: SHA-256 body dedupe at the gateway — identical bytes within 60 s → 409 duplicate. Catches network-level retries/double-sends; separate recordings differ by design. Per-instance best-effort like the rate limit.
- Both changes typechecked and exercised in Node (bad key 401, missing/stale/garbage timestamp 401, fresh passes, identical resend 409, new body passes).
- NOT committed/pushed. ⚠️ Deploying is a breaking change for the current Shortcut: add the X-Timestamp header (iphone-shortcut.md step 2) at the same time as pushing, or every run 401s.
- Log-mining check: last night's 18:xx misses (系统重置, 巴西 → chat) all predate f23f28e, whose variants are live (service restarted ~21:30). Nothing new to add. No NEWS_HEADLINES runs since the 沙田醫院 hallucination — still watching.
- User reported mid-session: BUS_TIMES had no Data on Notion — its runner never returned structured data (only weather/fuel/bin did). _bus_times now returns (reply, data) with stop + top-3 {route, minutes}; verified directly (Glenfield Mall, 906 即刻到). User restarted the service by voice, so it's live. TIDE_TIMES got the same treatment on request right after ({port, events[{type, time, height_m}]}, verified directly) — every skill command now returns structured data.
- Deployed: user approved commit+push; gateway hardening + bus data live, X-Timestamp check verified in production (curl probe got the new 401), user updated the Shortcut. First real WEATHER_COMPARE spoken by voice (天氣比較 09:59). TIDE_TIMES data live after voice restart (潮水 10:40 with events).
- Homepage: added "Translate to English" Google Translate link via ops/generate_homepage.py (survives hook regeneration; verified 302 to the translate.goog proxy). Google's embeddable widget is discontinued — the link was the right option.
- Log mining round 3 (user request after 6 failed tide attempts 10:38–10:40): added TIDE_TIMES variants — 潮漲潮退/何時潮漲/何時潮退 (natural forms), 超值/超值時間/何時朝退/幾時朝章/撤退 (observed mishearings; 撤退 is a real word but the command wins, 巴西 precedent), simplified 潮涨/潮汐时间/潮涨潮退. All verified to route.
- ASR benchmark built (user asked about Whisper fine-tuning; agreed benchmark-first): server.py gets opt-in ASR_CAPTURE_DIR (saves request audio + transcript locally, warning logged when on); ops/asr_bench.py transcribes captured clips with each model (same prompt/VAD as service, CPU by default to spare the GPU) and reports command-routing accuracy, exact match, CER. Candidate JackyHoCL/whisper-small-cantonese-yue-english-ct2 (Cantonese+English code-switching fine-tune, ready CT2 — no torch needed) downloaded and smoke-tested alongside medium. Real run needs the user to capture ~20 spoken phrases first. Fine-tuning on own voice deferred: 4 GB GPU can't train; only if the community model disappoints.
- Benchmark ran on 12 real phone clips (capture setup hit two snags: user's first registry paste ran lines in reverse order — fixed with a one-liner; and the first phrase pass was all 13 phrases in one recording — needs one per Shortcut run). Two ambiguous labels (發洩, C.O.T.O.) resolved by second-opinion transcription with the candidate (巴士, 潮退) + phrase-sequence position; user should flag if wrong.
- RESULT: JackyHoCL/whisper-small-cantonese-yue-english-ct2 beats medium decisively on command routing 83% vs 58%, exact 75% vs 50%, CER 0.17 vs 0.54, ~3x faster (2.6 vs 7.4 s/clip CPU). Its only misses were one-char near-misses 油家/潮推 — added as variants (+朝夕 observed 10:58), after which it routes 12/12. medium's misses were unusable (發洩, 確認(!), 散漫, 巴適, C.O.T.I).
- Caveats: 12 clips, one speaker, commands only — code-switching and long chat utterances untested (though the fine-tune trained on mixed Canto-English speech). Recommended switching ASR_MODEL to the fine-tune; keep capture on until confirmed in daily use, then remove ASR_CAPTURE_DIR + delete asr/logs/capture (log-hygiene item).
- User switched production: ASR_MODEL=JackyHoCL/whisper-small-cantonese-yue-english-ct2, service healthy on CUDA. README gained the mishearing-challenge/benchmark-strategy story; lesson saved to memory (asr-mishearing-lessons).
- Post-switch verification by replaying the bench clips through the live service found a subtle trap: GPU int8_float16 transcribes marginal clips differently from the CPU int8 bench — 潮汐 clip came out 潮漬 live (超值 in the bench), 油價 clip 油㗎 (油家 in bench). Both added as variants; all replayed clips now route. Lesson: after a model/precision change, replay the test clips through the LIVE service, not just the bench.
- MILK_PRICES built (planned in plan mode from a user-pasted over-engineered "price intelligence" spec, scoped to: cheapest standard 3L per store + briefing-only drop alert): grocer-nz skill via new SQL `query` subcommand, stores 45/246/315/365 (Woolworths Glenfield, PAK'nSAVE Albany, New World Albany+Birkenhead), product whitelist 5449/4068/5506/5456 (Pams/WW/Simply standard + Anchor Blue Top fallback), env-overridable MILK_STORES/MILK_PRODUCTS. Reply dedupes chains (cheapest branch spoken, all in data). _milk_drop_line() in MORNING_BRIEFING speaks only when cheapest < last pre-today grocer history row — stateless, no local storage. duckdb+pytz installed into asr/.venv (grocer CLI's uv auto-bootstrap crashes 0xC0000005 under system Python). Gotchas: CLI caches parquet forever → must pass --refresh, and it's a main-parser flag (before the subcommand). Verified: real prices (PnS $7.09 cheapest), drop branch via monkeypatched history, all phrases route, briefing 21 s with milk line correctly silent. grocer.nz has no Costco; "Countdown" is Woolworths now. Deferred by user: Δ-vs-yesterday in reply, 30-day trend command, always-on briefing line.
- MILK_PRICES live-verified by voice first try (牛奶價錢 transcribed perfectly on the new model — four-syllable phrases are its comfort zone).
- NEWS data now carries {title, url, source} per headline (user request) so the Notion Data column links to articles; 881 chars, under Notion's 2000 cap.
- Name-hallucination guard shipped (watch item triggered — Bay of Plenty→灣仔埗, second incident): _lost_names() requires mid-sentence capitalized words to survive translation verbatim (allowlist: New/Zealand/NZ/Auckland/Wellington/Christchurch which gemma translates correctly; sentence-initial position and post-quote/colon capitals skipped as unjudgeable; leading multi-word names caught via later words). On loss: retry once with names pinned, then English-title fallback. Live run: retry couldn't save "Plenty", English fallback spoken — accuracy beats fluency. 16.5 s for 3 headlines incl. one retry.
- User voice-tested the guard: all 3 headlines fell back to English — the survival check can't tell correct translations (Papua New Guinea→巴布亞新畿內亞, Beach→海灘, PM→首相) from hallucinations (Marianas→馬來亞), so it rejected everything. Replaced validation with prevention: _mask_names() swaps proper-name spans for 【N】 placeholders before translation and reinserts the English after — the model can't hallucinate what it never sees. Three rounds of gemma-wrangling to make it stick: one-shot example in the prompt (subject placeholders got paraphrased away as 佢/政府), left-to-right placeholder numbering (right-to-left numbering made it echo 【1】 twice), normalize half-width [1] brackets, strip model-invented extra placeholders. Two consecutive clean live runs: all names English-intact, all headlines Cantonese, 7.8 s, zero retries (vs 100% English fallback before). English-title fallback retained for placeholder loss after one retry.
- NZ place localization (user-provided dictionary): asr/nz_places.json holds ~45 English/Māori→繁體 place names in the user's four groups (cities/regions/islands/landmarks, cities loaded last so bare duplicates speak the city); _localize_places() applies longest-match with ASCII-letter guards (not \b — CJK is \w, and "New World"/"Aucklander" must never match) to every reply via _execute + chat fallback. Completes the translation story: curated NZ names speak Chinese (奧克蘭潮汐), the masking guard keeps unknown names accurately English. 10 edge-case units + live tide/milk/news all verified.
- Second translation batch triaged rather than integrated wholesale: added proper nouns (Aucklander/毛利/Pākehā, Auckland Transport/Airport, University of Auckland/AUT, The Warehouse/Chemist Warehouse, North Shore/South Auckland/CBD) + CJK gap-collapse after adjacent replacements; skipped annotation keys that can't match text ("Kiwi (person)"), generic nouns (Rain/Doctor/Bus — replies are already Cantonese, and word-substitution inside English news titles makes mush), and Taiwan-register terms (公車/計程車). Supermarket brands (新世界/沃爾沃斯/百佳 incl. PAK'nSAVE casing variant) added after asking — user chose translation despite the 百佳=PARKnSHOP collision note. Verified live: 「3公升牛奶：百佳 Albany最平7.09蚊…」.
- QUOTE_OF_DAY (16th command, user request): 今日金句/金句/星爺金句/quote → random pick from asr/quotes.json (95 Stephen Chow quotes, user-provided, kept verbatim incl. duplicates). Caught a definition-order gotcha: runner functions must be defined before the COMMANDS dict (~line 560), not near the reply post-processing helpers further down.
- Next: user restarts service to load today's remaining changes (news masking, place/brand localization, QUOTE_OF_DAY); daily-drive the new model with capture on; if chat quality holds for a day or two, remove ASR_CAPTURE_DIR + delete asr/logs/capture. Then hardening: credential isolation, file handling safety, secrets/audio out of logs; DEPLOY_HOOK_URL still unconfigured.

## 2026-07-15 (evening)
- MOVIE_QUOTE (17th command, 35acc03): 電影金句/電影對白/movie quote → random HK movie line from asr/movie_quotes.json, spoken as 《戲名》，角色話：對白. User supplied two overlapping datasets (one flat, one per-movie dict with a JSON syntax error); per user's choices: merged both, deduped by movie+quote, dropped every （場景描述）/（概述） placeholder that isn't actual dialogue → 72 entries. Kept separate from the Stephen-Chow-only QUOTE_OF_DAY. Voice-verified after restart.
- Next-level brainstorm triaged (user pasted a generic tier-2 roadmap): rejected barge-in/wake-word (Shortcut client is push-to-talk), plugin manifests (COMMANDS dict suffices), voice biometrics, voice-first app UIs. Ranked keepers: push channel → bounded agents → follow-up context (allowlist-resolved, never LLM-routed) → semantic history search. User chose automation-first.
- Gateway JSON text path (1fd43b0): ?mode=command accepts {"text": "早晨"} — skips ASR, same auth/rate-limit/dedupe guards, 415 without mode=command; server caps text at 500 chars. Briefing runs log to history.jsonl → Notion like spoken commands. Verified end-to-end through production (系統狀態 executed via public gateway).
- Testing gotcha: replayed a captured clip to verify the audio path still worked — the clip was the user's 重啟語音系統 recording and it executed a second restart. Captured clips are live commands; RESTART_ASR carries no 確認 guard. Lesson: never test with real command audio (or consider flagging RESTART_ASR destructive).
- Push channel via ntfy (0d74d90), replacing the earlier Pushover plan (user installed the free ntfy app instead of the $5 purchase): ops/notify.py — notify(title, message, priority 1-5), RFC 2047-encoded Chinese titles, topic in gitignored ops/ntfy.json (the topic IS the credential — 128-bit random suffix), silent no-op unconfigured (notion_sync pattern). Test push received on the phone. Caveat noted: ntfy.sh caches messages plaintext ~12 h — keep future DeMark alert content vague.
- Spoken morning briefing: new iphone-shortcut.md section — 早晨簡報 shortcut (JSON body text=早晨 → Speak reply, error branch) + personal automation. User built and manually tested it; daily 10:00 trigger, first unattended run 2026-07-16.
- healthchecks.io → ntfy wired (via browser automation): ntfy integration on the project, down=high priority/up=default, auto-assigned to the Voice OS check alongside email; dashboard test push delivered to the phone. Pipeline-down alerts now buzz the phone instead of email-only.
- First bounded agent live (cfb668b): ops/milk_watch.py — reuses the briefing's _milk_drop_line() (today's cheapest standard 3L vs last pre-today grocer history price), pushes 牛奶減價 via notify(), logs to asr/logs/milk_watch.log. "VoiceOS Milk Watch" scheduled task daily 9:00 as the user (venv python, notion_sync pattern); scheduler-triggered run verified ("no drop"). Drop days double up with the 10:00 spoken briefing — accepted.
- Homepage gains a static 自動功能 (non-voice) section listing the four automations: 10:00 spoken briefing, 9:00 milk watch, 10-min heartbeat→phone, 5-min Notion sync.
- Next: confirm the 10:00 automation fired unattended (Run Immediately is the usual failure point); DeMark agent via the milk_watch pattern when ready; then capture cleanup + hardening items (credential isolation, file handling, log hygiene); DEPLOY_HOOK_URL still unconfigured.

## 2026-07-16
- 10:00 spoken morning briefing fired unattended and spoke on the iPhone (user-confirmed; history.jsonl 10:00:24 MORNING_BRIEFING) — prior session's open item closed.
- MILK_PRICES phrase gaps fixed from service.log: user tried 牛奶 (twice) and 牛奶價錢→misheard 牛奶加錢 before 比較牛奶 matched; all three fell to chat. Added 牛奶 / 牛奶加錢(加钱) / milk variants (asr/router.py), per the mine-real-mishearings approach.
- MORTGAGE_RATES (18th command): 利率/按揭利率/供樓利率/mortgage rates → interest-co-nz TheColab skill (interest.co.nz table), cheapest 1-year rate per big-five bank (special beats standard), ranked reply e.g. 一年定息按揭：ANZ最平4.65厘…. Institution strings carry marketing junk — substring-matched to ANZ/ASB/BNZ/Westpac/Kiwibank; CLI default --limit 30 truncated before Westpac, runner passes --limit 200. Full rate table stored in history data. DESCRIPTIONS entry added (ops/generate_homepage.py).
- Restarted VoiceASR via its own RESTART_ASR text-path command (SCM restart needs elevation); live-verified 牛奶加錢→MILK_PRICES and 利率→MORTGAGE_RATES through /command.
- Changes uncommitted — pre-commit hook regenerates homepage + CLAUDE.md command list on commit.
- Next: capture cleanup, credential isolation / file handling / log hygiene, DEPLOY_HOOK_URL.
- Capture cleanup done (afternoon): ASR_CAPTURE_DIR removed from VoiceASR AppEnvironmentExtra (user ran the elevated one-liner), service restarted via RESTART_ASR text path, no "capture mode ON" at startup, asr/logs/capture (141 files, 6.2 MB) deleted. Partially closes the log-hygiene checklist item.
- Homepage dashboard shipped (a394d30): auto-counted 語音指令/自動功能 stat tiles; automations moved into an AUTOMATIONS list in ops/generate_homepage.py; pre-commit hook now also fires on generate_homepage.py changes. Live-verified by user.
- Credential isolation shipped (evening): SECURITY.md written (threat model + measures); ops/harden_voiceasr.ps1 switches VoiceASR to virtual account NT SERVICE\VoiceASR (nssm.exe copied to C:\Program Files\nssm — WinGet copy is in the user profile; SeServiceLogonRight via secedit; GROCER_NZ_CACHE/TMP/TEMP redirected to asr\cache; deny ACEs on notion.json/ntfy.json/gateway .env). First run failed silently at sc.exe config (PowerShell quote-mangling, output swallowed) — fixed by writing ImagePath/ObjectName via registry; script is idempotent. Verified: service healthy as NT SERVICE\VoiceASR on cuda, SYSTEM_STATUS + MILK_PRICES live (grocer cache re-downloaded to new path), deny ACEs present. CLAUDE.md checklist item ticked.
- Residual risk documented: Authenticated Users has inherited Modify on D:\ai, which service tokens include — service can write repo code/ops scripts (escalation path). Tightening (break inheritance) proposed as follow-up. Cloudflared still LocalSystem.
- ACL tightening (ops/tighten_acls.ps1): inheritance broken on D:\ai\voice-ecosystem + D:\ai\thecolab-skills, Authenticated Users Modify removed, timlo explicit Full Control. Service now RX-only on code, M only on asr\logs + asr\cache; verified live (SYSTEM_STATUS + MILK_PRICES) and git works. Authenticated Users residual risk closed in SECURITY.md.
- Transcript retention decided (user chose looser option) + enforced: chat entries 30d, command entries forever (Notion mirrors), rotated service-*.log 90d. ops/prune_logs.py rewrites history.jsonl atomically only within the Notion-synced region and shifts the byte-offset cursor by bytes removed (tested on synthetic data: correct drop, cursor still points at end of synced region, idempotent). "VoiceOS Log Prune" task daily 03:32 as user (offset from 5-min sync marks); manual run verified. 5th automation added to homepage AUTOMATIONS; SECURITY.md retention table; CLAUDE.md log-hygiene item annotated (remaining: Vercel/Cloudflare header audit).
- File handling safety audited — satisfied by design, no code changes: filenames never read (upload.read() only; gateway forwards raw bytes), in-memory BytesIO->PyAV decode (no ffmpeg CLI/shell), _run_skill arg lists with owner-set/int-validated values, size+type validated both hops, duration capped pre-inference. Checklist ticked; audit section added to SECURITY.md.

## 2026-07-17
- Reminder alerts shipped (user request: "ntfy alert at the designated date/time" — the iOS Reminder the Shortcut creates has no alert, so nothing ever fired). New `ops/reminder_alerts.py` + "VoiceOS Reminder Alerts" scheduled task (every minute, runs as timlo — same credential isolation as Notion sync; the router still can't read ntfy.json). Byte-cursor scan of history.jsonl for executed CREATE_REMINDER entries → pending list in asr/logs/reminder_alerts.json → notify() when due; failed pushes retry, >24 h overdue drops with a log line (protects first run over old history). Verified: real ntfy push received; task self-ran (exit 0) and queued tonight's reminder.
- Reminder extraction fixed (service.log showed 提我今晚六點半… → due null error; 今晚六點正 → 21:00 with time words in title). Prompt now spells out 24 h rules (今晚/夜晚=PM, 半=:30, 正=:00) + two few-shot examples with computed dates (asr/router.py _extract_reminder). Benchmarked against Ollama on the real failed utterances: old prompt 2/8, new prompt 12/12. Gotcha: first example ("食飯") taught gemma3 to over-trim titles — dropped 太和; example changed to keep non-time detail.
- VoiceASR restarted via RESTART_ASR text path; live-verified the exact failing utterance → 去太和食飯（7月17號夜晚6點30分）, alert queued for 18:30 tonight. The morning's mis-extracted 21:00 pending entry removed from state so no wrong alert fires.
- CLAUDE.md CREATE_REMINDER description updated. Changes uncommitted.
- 6th automation added to ops/generate_homepage.py AUTOMATIONS (homepage regenerates on commit via the hook).
- Next (unchanged): DEPLOY_HOOK_URL (marker-file pattern), Vercel/CF header audit, Cloudflared account move.
- Slack bridge shipped (afternoon; user brought a ChatGPT CopilotKit blueprint — rejected in planning: LLM free-form action calling violates the fixed-enum prompt-injection guard, assumed a DeMark engine/`/speak` endpoint that don't exist, and CopilotKit is a React UI framework; user approved the thin-bridge alternative). New `gateway/api/slack.ts`: Slack signature verify (HMAC v0, ±5 min, timing-safe) → url_verification handshake → app_mention events only (bot_id loop guard, X-Slack-Retry-Num acked-and-ignored to prevent double-execution) → mention stripped → 3 s ack, then command runs via `waitUntil` (`@vercel/functions`, gateway's first runtime dep) → POSTs `{"text": ...}` to our own `/api/voice?mode=command` (reuses auth/rate-limit/dedupe/CF Access) → reply (or error) posted back via chat.postMessage. vercel.json maxDuration 60 for slack.ts. Typecheck clean; 9/9 harness tests (signed real-handler requests, stubbed fetch). DeMark explicitly out of scope — future COMMANDS entry works from Slack automatically.
- NOT yet deployed: needs commit+push (user confirmation), then Slack app creation (workspace exists, no app) — scopes app_mentions:read + chat:write, event subscription to /api/slack, SLACK_SIGNING_SECRET + SLACK_BOT_TOKEN into Vercel env + gateway/.env, then end-to-end @bot 系統狀態.
- Slack bridge LIVE (15:35 end-to-end: @mention milk → MILK_PRICES → prices posted back in #bot-channel; workspace "Neo AI"). Deploy + setup hurdles: Vercel GitHub integration silently ignored the push (manual redeploy needed; CLI `vercel --prod` blocked by session permissions — user deployed); Slack Event Subscriptions initially never saved (greyed Save); missing_scope on chat.postMessage until chat:write added + reinstall. Synthetic signed app_mention against /api/slack + `vercel logs --json` isolated each leg. SLACK_* env vars in Vercel + gateway/.env. Retry-guard under a slow command (早晨) not yet exercised.
- Post-go-live polish (098bd6c, deployed via Git integration this time): (1) Slack dedupe wart fixed — identical phrases within 60 s were byte-identical bodies and got 409'd; bridge now sends slack_event_ts in the body (ASR server ignores unknown keys; true Slack retries reuse the ts so still dedupe). User-verified: two "milk" mentions 2 s apart both answered. (2) Channel tagging — record_history takes source (voice/text/slack); service.log logs "command [slack]: ..."; slack.ts sends source:"slack"; live-verified both. (3) All four python scheduled tasks switched to pythonw.exe — the 1-min reminder task was flashing a console window every run (Interactive logon + python.exe = visible console; pythonw is the fix, logs unaffected).
- Phrase mining from Slack: user typed "reminder me today to leave home at 6PM" → fell to chat (gemma3 chatted back, no reminder). Added "reminderme" to REMINDER_PREFIXES (c961918), restarted, replayed the exact utterance → CREATE_REMINDER, due 18:00 today, ntfy alert queued (alongside the 18:30 dinner one). Note: Slack-created reminders are ntfy-only — no iOS Reminder without the Shortcut in the loop.
- Docs brought current (user request "update everything includes README.md"): README.md (commands list incl. reminders/milk/mortgage/quakes/quotes, Slack bridge + channel tagging, automations fleet, two new Challenges: iOS dynamic-alert limitation → ntfy task, Slack 3s-ack/dedupe); SECURITY.md (Slack bridge surface section, Slack secrets + reminder-alerts task in credential inventory); CLAUDE.md history bullet (source field).
- Integration pattern ("plugin system") + Google Calendar reference shipped. User pasted an industry-plugin-system blueprint (manifests/permission enums/per-user OAuth DB/LLM tool wrappers); planning cut the multi-tenant machinery and the LLM-calls-tools part (fixed-enum guard), kept the applicable rigor: least-privilege scope, data minimization, per-integration logs, friendly error mapping. Deliverables: SECURITY.md "External integrations" section (reads = user-context sync task → sanitized cache; writes = marker-file + 確認; rules list), ops/google_calendar.py (stdlib OAuth loopback --auth + 10-min sync of primary calendar → asr/cache/calendar.json, titles+times only), "VoiceOS Calendar Sync" task (pythonw), TODAY_AGENDA 19th command (今日行程) + briefing agenda line (fresh-cache-only, silent skip), ops/google.json skeleton (gitignored, deny-ACLed, added to harden script), homepage DESCRIPTIONS/AUTOMATIONS. Verified: compile ok, unconfigured no-op logs+skips, task Ready, restart + 今日行程 → 行程功能未接通Google日曆. Awaiting user OAuth (Google Cloud project, consent PUBLISHED to production — Testing status expires refresh tokens in 7 days — then --auth), then live verification.
- Calendar integration DEFERRED by user (believes a paid Google account is needed). Code/docs/pattern stay shipped (b9cccca); "VoiceOS Calendar Sync" task disabled until resumed; 今日行程 replies the not-connected line by design. Resume steps in memory (next-session-plan).
- Workflow layer shipped (free from-scratch IFTTT replacement; user rejected IFTTT on cost, asked for free options — iOS Shortcuts automations noted as the free path for phone-context triggers). ops/workflows.py + ops/workflows.json (committed, owner-edited): triggers = schedule (once/day, late catch-up) + history (cursor); conditions = reply_contains / data_contains / data_gte / data_lte with {today}/{tomorrow} tokens ("17 July" format matches skill dates); actions = ntfy / non-destructive allowlisted commands (destructive refused — never arms the 確認 window; resolved via router import) / webhook. "VoiceOS Workflows" task every minute (pythonw). server.py: source "workflow" accepted + /command keeps data in responses for workflow source only (bug found in testing: data was popped so conditions never saw rain_prob). Seed rules: umbrella-alert (07:30, rain_prob≥70), bin-night (20:00, rubbish contains {tomorrow}), command-error-alert (history status=error). All verified live: data condition matched {today} and pushed, destructive refused, error trigger pushed; workflow-sourced entries excluded from triggers (loop guard).
- Reminder prompt regression found+fixed while testing: bare 提我 made gemma3 parrot the few-shot example (去銀行交錢 18:30) as a real reminder — deterministic guard in _create_reminder now errors on bare prefixes before the LLM is asked; parroted junk removed from the alert queue.
- "IF I leave home and rain starts THEN grab my jacket" implemented: server half = JACKET_CHECK 20th command (帶唔帶遮/出門檢查 — Open-Meteo current weather_code ≥51 = raining now; max precipitation_probability over next 2 h ≥60 = imminent; the no-rain reply deliberately avoids 帶 so the phone can branch on it). Three branches unit-verified with mocked payloads + dry branch live. Phone half documented in iphone-shortcut.md: 出門檢查 shortcut + "When I Leave Home / Run Immediately" automation (iOS 17+) — notification+speech only when the reply contains 帶. User still needs to build the automation on the phone.
- End-of-day state (17:30): ASR healthy on cuda (Cantonese fine-tune); 20 commands, 8 automations. Scheduled tasks all last-result 0: Log Prune 03:32, Milk Watch 09:00, Notion Sync 5-min, Reminder Alerts 1-min, Workflows 1-min; Calendar Sync disabled (deferred). Heartbeat false alarm during the sweep: "VoiceOS Heartbeat" is invisible to non-elevated task queries (registered as SYSTEM from an elevated shell) but healthchecks.io shows pings every 10 min, up, 98.37% July uptime, email+ntfy integrations ON — do NOT re-register it when a non-admin listing says it's missing. README refreshed (jacket check + workflows). Pending user actions: build 出門檢查 leave-home automation (recipe in iphone-shortcut.md — user says will test later today); reminders 18:00 去家 + 18:30 去太和食飯 should push tonight; umbrella rule's first natural evaluation tomorrow 07:30.

## 2026-07-18
- 10AM briefing timeout diagnosed (user report): server completed MORNING_BRIEFING fine at 10:00:35 (~35 s, history executed) but the iPhone Shortcut's ~30 s Get-Contents timeout fired first; gateway 55 s / Vercel 60 s never hit. Root cause: no keep_alive in any Ollama payload, so gemma3:4b unloads after Ollama's 5-min idle default — first headline translation paid a 15-20 s cold weight reload (the 07:30 umbrella workflow is weather-only, nothing kept it warm), plus one placeholder retry.
- Fix: "keep_alive": "24h" on all four Ollama call sites in asr/router.py (warm_ollama, headline translation, reminder extraction, chat fallback) — the startup warm pins the model and every call renews the window. Restarted via RESTART_ASR text path; ollama ps shows UNTIL 24 hours; timed 早晨 run 20 s (was ~35 s), inside the phone window. Real-world proof = tomorrow's 10:00 automation.
- Speed benchmark (user request): existing (4b, ctx 8192) vs num_ctx 2048 vs gemma3:1b-for-tasks split, using the exact production prompts (scratchpad script, 18 timed calls/config). Existing won. ctx2048: no gain — CPU/GPU split barely moved (54/46 vs 56/44; the 3.3 GB of weights overflow the 4 GB GPU, not the KV cache) plus a 17.8 s outlier. 1b split: extraction 3x faster but 0/6 usable (malformed dates like "2026-07 - 19, 21:00"), translation placeholders 3/8 kept, and a 20 s GPU-contention outlier with both models resident. gemma3:1b deleted afterwards (user request).
- Benchmark side-finding fixed (user request): 4b resolved 禮拜五 to Saturday's date 6/6 runs despite the prompt calendar naming the Friday date. New _snap_weekday() in asr/router.py: deterministically snaps the extracted due date to the next occurrence of the spoken weekday (禮拜/星期/周/週 + 一-日/天, 1-7 days out, same semantics as the prompt calendar), keeps the model's time, leaves 下個禮拜X (next week) alone. 6/6 tests passed incl. live extraction of the failing utterance → Friday 07-24 10:30. Restarted + warmed clean.
- Committed 38516a5 (both changes, one router.py commit) and pushed on user request; pre-commit hook regenerated the homepage — no diff (no new commands). Vercel redeploy is a no-op (gateway untouched).
- Afternoon verifications (user confirmed): 07-17 evening ntfy reminders fired; 出門檢查 leave-home automation built and ran. Slack retry guard exercised with a 5-mention 早晨 burst at 13:36: guard PASSES (zero duplicate replies; slack_event_ts dedupe treats identical rapid mentions as distinct commands) — but 3 of 6 replies silently dropped. All 6 executed server-side (history.jsonl), so the loss was inside slack.ts after the router replied: briefings serialize behind Ollama (~20 s each), later events outlived the 60 s maxDuration before their reply (or error) posted.
- Slack burst fix shipped: vercel.json slack.ts maxDuration 60→300; postToSlack retries once (HTTP 429 Retry-After honoured); per-channel rate limit 3 command executions/min (user suggestion) — over-limit mentions get an immediate 指令太密 reply, so nothing fails silently. 13/13 harness checks against the real compiled handler (signed requests, stubbed fetch): normal flow, the exact 5-burst (3 executions + 2 throttle notices = every mention answered), cross-channel isolation, retry deliveries still ignored, both postMessage retry paths, bad signature 401. Docs updated (README, SECURITY.md Slack surface, CLAUDE.md bridge bullet).
- TRIGGER_DEPLOY wiring (DEPLOY_HOOK_URL via marker-file pattern) removed from the backlog — user decision; the command stays an unarmed stub in the router.
- Burst fix deployed (e115424, user confirmed) and verified live twice: (1) 5 synthetic signed mentions to a fake channel → exactly 3 reached the router, 2 throttled; (2) user's real 4-mention burst → 3 replies posted (no drops) + visible 指令太密 notice on the 4th. Cross-channel isolation confirmed — both bursts shared a minute without consuming each other's allowance. Bonus finding: the user's mentions carried a quoted copy of an earlier message ("早晨\n[1:36 PM] 早晨"), so they correctly fell to chat, not MORNING_BRIEFING — exact-match allowlist working as designed. First-line-only command matching offered for quoted mentions; not requested yet.
- ntfy audit (user question): two logs. reminder_alerts.log — 07-17 alerts 去家 18:00 + 去太和食飯 18:30 both sent (matches phone); pending 出門 today 18:40, Hot Point 07-30 09:00. workflows.log — umbrella rule's first natural 07:30 run evaluated correctly (condition not met, 0% rain; last open verification item closed); command-error-alert pushed at 10:17 for an errored 提我.
- Known-wrong reminder LEFT AS-IS (user decision): 「提我下個星期五十點鐘早上用哂啲hot point」→ due 2026-07-30 09:00 — wrong twice (07-30 is a Thursday; should be Friday 07-24, and 早上十點 became 09:00, the few-shot example's time). Extracted at 10:17, pre-snap-fix — but _snap_weekday deliberately skips 下個星期X phrases, so it wouldn't have caught it. Extending the snap to 下個星期X was offered; user declined for now ("leave them").
- Next: tomorrow 10:00 briefing on the phone is the keep_alive real-world proof; 出門 ntfy due 18:40 tonight. Backlog: Vercel/CF header audit, Cloudflared off LocalSystem. Ideas offered but not approved: 下個星期X snap extension, first-line command matching for quoted Slack mentions.

Evening session (chat persona + ASR prompt fix; commits c9f286d…5c59ab2, all pushed):
- 周星馳 chat persona set (user-provided prompt, verbatim) as OLLAMA_SYSTEM_PROMPT (c9f286d). Verbatim it was unusable — 600+ char replies, 38–50 s on the 4 GB GPU, every call past the 30 s Ollama timeout — so one clearly-marked technical rule appended: replies are spoken, max three sentences, no parenthetical stage directions. Result 12–17 s, in character.
- Stage directions still leaked (gemma3:4b ignores the prompt ban) → code-side strip of （…）/(…) spans in _ollama_fallback (9063226); TTS no longer reads actions aloud.
- Multi-turn chat memory (4d5b06f): _recent_chat_turns() reads the tail (64 KB) of history.jsonl and injects the last 5 chat exchanges within 30 min (all channels share one thread; commands excluded) into the Ollama messages. Verified mechanically (10 messages injected) and conversationally. Watch: worst case 23.6 s — if chat_error shows up in daily use, drop max_turns first.
- quotes.json blended into the persona (d9ca6a3): _hour_quotes() picks 5 of the 95 lines with an hour-stable seed (keeps Ollama's KV prefix cache warm within a conversation; per-call shuffle would re-eval the whole persona every reply). Two more deterministic guards proved necessary: tail-strip of verbatim hour-quotes dumped at reply ends, and self-parrot dedupe (drop ≥15-normalized-char sentences that verbatim-repeat an injected past reply — the 阿華田 riff had hit 100% of replies once memory landed; short catchphrases exempt). Side effect: latency down to 9–15 s. Known behaviour: repeating the same question within the memory window can leave a stubby reply after dedupe.
- Real bug found while fixing a mishear: Whisper initial_prompt had grown to 880 tokens against the ~223-token window — the first three-quarters of command vocabulary (系統狀態…今日行程) had silently lost recognition bias; only the tail was covered. Fixed (693aec0): one canonical CJK phrase per command (router still matches all variants — they exist for matching, not biasing) = 152 tokens, all 20 commands back in the window, plus chat vocab 人最緊要係尊嚴/夢想鹹魚 line (user said 尊嚴, Whisper wrote near-homophone 專業). Token counts measured with the deployed model's own tokenizer.
- Phrase mining from live test: 老豆 heard as 老代 → 老豆即係爸爸 added to prompt vocab (5c59ab2, 158/223 tokens). Unconfirmed suspect: 「取消鹹魚」 at 17:54 may have been 除咗鹹魚 (取消 is the cancel keyword — harmless, cancel needs exact match; ask user before adding).
- Personal Whisper fine-tune discussed → BACKLOG "some day" (user decision; full recipe in memory next-session-plan): capture mode → ~100+ corrected clips → Colab T4 fine-tune from the JackyHoCL checkpoint → CT2 convert → asr_bench.py → ASR_MODEL swap. Trigger: when prompt-vocab fixes stop covering real mishearings.
- Next: user to voice-verify 尊嚴 + 老豆 land correctly; watch chat latency (30 s timeout headroom is thin with 5-turn memory); morning-briefing keep_alive proof still due tomorrow 10:00.
## 2026-07-19
- movie_quotes.json accuracy pass (session was cut mid-way by an API error; resumed and finished): replaced invented/paraphrased lines with the real canonical quotes where an iconic one exists — 無間道 (而家我想做一個好人 / 三年之後又三年), 英雄本色 (爭返一啖氣 / 我就係神), 重慶森林 (過期 monologue), 大話西遊 一萬年 monologue, 少林足球 鹹魚, 食神 (只要有心，人人都係食神), 功夫 (唯快不破), 阿飛正傳 (無腳雀仔), 花樣年華 (樹洞), 旺角卡門 (烏蠅 英雄 line), 春光乍洩 (不如我哋由頭嚟過), 九品芝麻官, 精武門 (我讀得書少), 掃毒 (段坤我食硬咗), 破‧地獄 (生人都需要破地獄), 秋天的童話 (命裡有時終須有), 寒戰 遊戲規則 speech, 無雙, 甜蜜蜜, 新不了情, 東邪西毒, 倩女幽魂, 葉問→葉問2 for the 人格無貴賤 speech.
- Metadata fixes in the same pass: 破‧地獄 year 2015→2024 (character 道生), 老笠 2012→2016, 唐牛→史提芬周 (食神), 常家駒→常歡 (家有囍事), 甜蜜蜜 second line reattributed 黎小軍→李翹, 旺角卡門 阿華→烏蠅, 春光乍洩 黎耀輝→何寶榮, 少林足球/功夫 speaker corrections; 無敵鐵沙掌 entry replaced by a second real 精武門 line.
- Films without an iconic one-liner keep their original generic lines (暗戰, 神探, 竊聽風雲, 賭神/賭聖, Jackie Chan titles, etc.) — deliberately not fabricating canon. Known pre-existing duplicates left as-is: 權力唔係用嚟炫耀 (寒戰+寒戰2), 出得嚟行，預咗要還 (古惑仔+非常突然).
- 72 entries, JSON validated, keys intact. MOVIE_QUOTES loads at router import, so RESTART_ASR was triggered via the gateway text path and 電影金句 verified live post-restart. Committed and pushed.

Afternoon session — GENERATE_IMAGE (21st command, Slack-only local image generation; planned via /plan from a user-pasted blueprint):
- Blueprint's architecture didn't fit (assumed a local Node Slack bot hitting localhost:8001) — rebuilt on the existing path: slack.ts → voice.ts → tunnel → router → asr/image_gen.py subprocess (LCM Dreamshaper v7 via diffusers, CPU fp32, 512px, 6 steps) → PNG to asr/cache/images (newest 20 kept) → base64 in data → slack.ts uploads via the external upload flow (files.upload is sunset) + posts the text reply. Credential isolation holds: the box never sees a Slack token; the bridge never sees the model.
- User decisions: Slack-source only (voice/text/workflow get a Cantonese refusal — enforced in the router, verified); CPU not GPU (protects the gemma3:4b keep_alive pin — ollama ps confirmed 24h window intact after generation); LCM Dreamshaper v7 over sd-turbo (quality over speed).
- Prefix routing like CREATE_REMINDER: 畫/draw/image via raw-text regex (\b guards draw/image so imagine doesn't trigger; CJK has no \b — 畫面 chat is knowingly captured, Slack-typed only). COMMANDS stub keeps LIST_COMMANDS/homepage/Whisper-prompt in sync.
- Timeout chain for ~60-100 s CPU generation: subprocess 210 s; voice.ts JSON-command upstream 240 s (audio keeps 55 s — a phone caller is waiting), maxDuration 60→300; slack.ts COMMAND_TIMEOUT_MS 58→250 s. Upload failure posts a visible 上載唔到 notice (never silent).
- Single-flight lock (user-requested rate limit): concurrent 畫 gets an immediate 畫緊上一張 reply — /command threadpool would otherwise run parallel ~5 GB fp32 subprocesses; Slack's 3/min channel throttle already caps volume. Verified live with a mid-generation probe.
- Found in verification 1: deps had to go into asr/.venv (service runs .venv\Scripts\python.exe, NOT system Python312 — the elevated Program Files install was wasted; harmless leftover). torch 2.13 CPU + diffusers 0.39; faster-whisper/ct2 unaffected (health on cuda after restart).
- Found in verification 2: huggingface_hub 1.x offline mode rejects diffusers' own pipeline-only download as an incomplete snapshot — image_gen.py resolves the cached snapshot folder itself (refs/main → snapshots/<commit>) and loads from disk; HF_HUB_OFFLINE=1 stays as belt-and-braces.
- Found in verification 3: SD1.5 CLIP doesn't understand Chinese — 一隻太空貓 drew a girl's portrait. Fix: _translate_image_prompt() — CJK prompts go through gemma (data-only, same guard as headline translation, fallback to raw on failure); data carries prompt + prompt_en. Verified: 一隻著住太空衣嘅貓喺月球上面 → "Cat in spacesuit on the moon" → correct image, 60 s end-to-end.
- History/Notion record a <N base64 chars> placeholder, never the blob (verified). Scopes now app_mentions:read + chat:write + files:write (user added + reinstalled). Docs: README, CLAUDE.md bridge bullet, SECURITY.md, homepage DESCRIPTIONS.
- Remaining: live Slack @mention test of the upload leg (needs the deployed gateway); optional cleanup of the wasted system-Python torch install (~2-3 GB on C:).
